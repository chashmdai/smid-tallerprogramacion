spring.application.name=SGS-Standalone
server.port=8083

# ==========================================
# CONEXIÓN A BASE DE DATOS (MySQL 8) - db_sgs
# ==========================================
spring.datasource.url=jdbc:mysql://localhost:3306/db_sgs?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=patsYned24
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==========================================
# JPA / HIBERNATE
# ==========================================
# Usa 'update' en desarrollo, pero en producción deberías usar 'validate' o Flyway
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=true

# --- THYMELEAF ---
spring.thymeleaf.cache=false
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8

# --- SEGURIDAD / APP ---
# Clave original
simd.security.encryption.key=SimdDefensoria26
# Clave inyectada para el @Value("${jwt.secret}") del SecurityConfig
# En el application.properties del microservicio SGS
jwt.secret=DefensoriaSmid2026SecretKeyHS256!

# --- OPENAI ---
openai.api.key=sk-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
# --- LOGGING ---
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.apache.tomcat.util.http.fileupload=DEBUG

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB